export default function LoadingComponent(){
    return (
        <div className='fixed inset-0 h-screen flex items-center justify-center font-ny p-20'>
            <span className="text-xs font-light uppercase text-gray-400 text-center tracking-[3px]">Loading</span>
        </div>
    )
}